import React from 'react';

function WordBubble({defArray}) {
    // const wordArray = defArray.map(word => {
    //     return <li>{word.word}</li> 
    // })
console.log("array", defArray)

  return (
  <div>
    {wordArray}
  </div>
  );
}

export default WordBubble;
